{
  "buildCommand": "npm run build && cp -r public/images/* dist/images/",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}